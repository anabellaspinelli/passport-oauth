<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Typeform: Test OAuth Scopes</title>
  <style>
    body {
      --accent: #d8495a;

      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      margin: 0;
      color: #262627;
      background-color: #fafafa;
    }

    nav {
      background-color: #ffffff;
      color: #4c4c4c;
      border-bottom: 1px solid #ededed;
      padding: 20px 10px;
    }

    nav ul {
      max-width: 960px;
      margin: 0 auto;
      padding: 0;
    }

    nav li {
      list-style-type: none;
      display: inline-block;
    }

    nav a {
      font-size: 18px;
      background: #F0F0F0;
      text-decoration: none;
      padding: 10px;
      display: block;
    }

    nav a:visited {
      color: inherit;
    }

    nav a:hover {
      background-color: #f5f5f5;
      cursor: pointer;
    }

    main,
    header {
      max-width: 960px;
      margin: 30px auto;
      padding: 0 10px;
      color: #333;
    }

    .btn {
      color: #fff;
      font-size: 15px;
      background: var(--accent);
      display: inline-block;
      border: none;
      padding: 3px 12px;
      min-width: 120px;
      padding: 10px;
      margin: 10px 0;
      text-decoration: none;
    }

    .btn:hover {
      cursor: pointer;
      background-color: #ef6474;
    }
  </style>
</head>

<body>
  <nav>
    <ul>
      <% if (user) { %>
        <li>
          <a href="/auth/logout">Log out</a>
        </li>

        <% } else { %>
          <li>
            <a href="/">Home</a>
          </li>
          <% } %>
    </ul>
  </nav>
  <header>
    <h1>Typeform OAuth: Test Scopes</h1>
  </header>
  <main>
    <p>A super simple app to test OAuth login flows and scopes</p>
    <% if (!user) { %>
      <a class="btn login" href="/auth/login">Select scopes and login through Typeform!</a>
      <% } %>
  </main>
</body>

</html>